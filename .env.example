# Affiliate program IDs — set these when approved for each network
NEXT_PUBLIC_AMAZON_AFFILIATE_TAG=verityaesthet-20
NEXT_PUBLIC_SEPHORA_AFF_ID=your-id

# Provider ingestion pipeline (scripts/ingest-providers.mjs)
GOOGLE_PLACES_API_KEY=
YELP_API_KEY=

# Optional: OpenAI for AI Concierge live replies
OPENAI_API_KEY=

# Contact form (server-side only — never exposed to the browser)
# Resend API key — https://resend.com/api-keys
RESEND_API_KEY=

# Server-side inbox for contact form delivery (optional — not shown on the site)
# CONTACT_EMAIL=your-inbox@example.com

# Public mailto address (optional — defaults to hello@verityaesthetics.app)
# NEXT_PUBLIC_CONTACT_EMAIL=hello@verityaesthetics.app

# Sender for Resend — verified domain preferred once verityaesthetics.app is verified in Resend
CONTACT_FROM=Verity Aesthetics <hello@verityaesthetics.app>
